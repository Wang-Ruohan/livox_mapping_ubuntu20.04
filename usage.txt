Use the following two statement to launch the file, and for noetic, you should type the Rviz base frame as "//camera_init"
roslaunch livox_mapping mapping_mid.launch
roslaunch livox_ros_driver livox_lidar.launch
